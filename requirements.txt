# The dependencies are declared once, in pyproject.toml. This installs the project itself,
# which brings them with it -- two lists of one thing is how the one nothing installs from
# goes stale.
-e .
